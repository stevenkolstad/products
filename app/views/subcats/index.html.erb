<p id="notice"><%= notice %></p>

<h1>Subcats</h1>

<table>
  <thead>
    <tr>
      <th>Subcat name</th>
      <th>Category</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @subcats.each do |subcat| %>
      <tr>
        <td><%= subcat.subcat_name %></td>
        <td><%= subcat.category %></td>
        <td><%= link_to 'Show', subcat %></td>
        <td><%= link_to 'Edit', edit_subcat_path(subcat) %></td>
        <td><%= link_to 'Destroy', subcat, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Subcat', new_subcat_path %>
